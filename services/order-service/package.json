{
  "name": "@microservices/order-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "clean": "tsc --build tsconfig.json --clean",
    "db:bootstrap": "node --import tsx ../../packages/platform/src/bootstrap-cli.ts --database-uri-env ORDER_MONGODB_URI --database-name-env ORDER_MONGODB_DATABASE --bootstrap bootstrap --application-name order-service-bootstrap",
    "start": "node dist/index.js",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  }
}
