{
  "name": "@microservices/notification-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "clean": "tsc --build tsconfig.json --clean",
    "db:bootstrap": "node --import tsx ../../packages/platform/src/bootstrap-cli.ts --database-uri-env NOTIFICATION_MONGODB_URI --database-name-env NOTIFICATION_MONGODB_DATABASE --bootstrap bootstrap --application-name notification-service-bootstrap",
    "start": "node dist/index.js",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "mongodb": "^7.6.0"
  }
}
